export class ColorScheme {
    
    /**
     * @param {string} name
     * @param {string} hex
     */
    setColor(name, hex) {
        switch (name) {
            case 'background':
                //console.log(hex)
        }
    }
}

